name: Eenmalig zoekveld onderhoudslocatie

on:
  push:
    branches:
      - development
    paths:
      - '.github/workflows/one-time-maintenance-location-search.yml'
      - 'scripts/maintenance-location-search.py'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: development
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Pas locatiezoekveld toe
        run: python3 scripts/maintenance-location-search.py
      - name: Test Machinepark
        run: npm test
      - name: Commit wijziging
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/workflows/one-time-maintenance-location-search.yml scripts/maintenance-location-search.py
          git add index.html sw.js tests/build-smoke.test.mjs
          git commit -m 'Maak onderhoudslocatie doorzoekbaar'
          git push origin HEAD:development
